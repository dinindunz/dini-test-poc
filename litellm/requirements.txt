# LiteLLM Server Requirements for Databricks Integration

# Core LiteLLM dependencies
litellm[proxy]>=1.40.0
uvicorn>=0.23.0
fastapi>=0.104.0
pydantic>=2.0.0

# Configuration and utilities
pyyaml>=6.0
python-dotenv>=1.0.0

# HTTP client for Databricks API
httpx>=0.25.0
requests>=2.31.0

# Logging and monitoring
structlog>=23.0.0

# Optional: For enhanced features
redis>=4.5.0  # For caching (optional)
prometheus-client>=0.17.0  # For metrics (optional)
